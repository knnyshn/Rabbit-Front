import React from 'react'
import NavBar from './Navbar/Navbar'

// Header with (POST MVP = functioning) search bar, logo, create post button, profile and login in menu.

export default function Header() {
    return (
        <div className='header'>
            <NavBar />
        </div> 
    )
}

